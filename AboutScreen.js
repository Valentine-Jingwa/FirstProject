import { Button, View, Text } from 'react-native';// Create a Details Screen
export default function AboutScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>This is the About page</Text>
      </View>
    );
  }